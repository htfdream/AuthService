spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:redispass123}
      timeout: 2000ms
test:
  from-prod-file: "YES_IT_WORKS"